# Django core
SECRET_KEY=django-insecure-srp-dev-key
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Database (MVP uses SQLite — no config required)
# Placeholder for future PostgreSQL migration
DB_NAME=srp_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432